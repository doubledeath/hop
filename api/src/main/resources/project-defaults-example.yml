swarm:
  ds:
    connection:
      url: url
    name: name
    username: username
    password: password
  keycloak:
    secure-deployments:
      api-1.0-SNAPSHOT.war:
        realm: realm
        auth-server-url: auth-server-url
        ssl-required: ssl-required
        resource: resource
        credentials:
          secret:
            value:
              value
    security:
      constraints:
        - url-pattern: /secured
          methods: [GET]
          roles: [god]